# Optional. Where gift icons and pack artwork are hosted; unset = grey placeholders.
# Images are never committed to this repository (they belong to Project Moon).
# VITE_ASSET_BASE=https://assets.example.com
