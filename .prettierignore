# Prettier is available as `npm run format`, but it is NOT a gate — `npm run check` does not run
# `format:check`, and CI does not either. The repository has never been formatted with it: running
# it today rewrites 142 files (+4347/-1712), and much of that is hand-aligned comment prose, tables
# and code samples that the tool reflows into something harder to read. Adopting it would be a
# deliberate, separate change with its own review, not a side effect of a cleanup milestone.
# See docs/review/M48.md.
dist
node_modules
data/raw
public/data
coverage
package-lock.json
